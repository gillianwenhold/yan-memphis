<div class='vw-100 vh-100 d-flex align-items-center justify-content-center'>
  <h1>Thank you for your response!</h1>
  <p class='lead'>Here's what others have been saying: </p>
  <hr class='my-4' />
  <% @poll.options.each do |option| %>
    <% amount = Response.where(poll_id: @poll.id, option_id: option.id).size %>
    <p><%= option.name %> (<%= amount %>)</p>
  <% end %>
  <br />
  <a href="<%= @option.link %>">Go to <%= @option.name %>!</a>
  <br />
  <% if @option.es_active %>
    <a href="<%= @option.link_es %>">Go to <%= @option.name %> (en español)!</a>
  <% end %>
</div>